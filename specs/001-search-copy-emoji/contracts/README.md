# Contracts: Search and Copy Emoji

**N/A** — This is a static client-side application with no backend API. There are no API contracts to define.

All data is sourced from bundled npm packages (`unicode-emoji-json` + `emojilib`) at build time.
